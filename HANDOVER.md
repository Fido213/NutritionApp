# EverydayFuel — Handover to Next Chat

**Date:** 2026-08-24
**From:** ox-alpha, pass 22
**Status:** Build + tests green (**262 tests, 18 files**). **Pass 22 = UI feedback round on top of pass 21 (both uncommitted):** **combo builder modernization + fixes** — ADD INGREDIENTS now lists the WHOLE library A–Z (the "can't see all my foods" bug was a hard 60-item cap; searches were capped at 40 → now 1000/300), **tapping a food that is already in the combo REMOVES it** (toggle, same as its × button), builder surfaces modernized (16px totals card w/ gradient wash, 12px rows, green focus ring on amount inputs, tinted ✓ In Combo badge). **Calorie ring = continuous gradient for the whole day** (no more flat class colors past the target): deep red empty → amber ~50% → green entering the band → deep green at target → red overtaking the green just past it → violet past the limit → purple intensifying to 3× over (`ring.ts` PALETTE anchors + lerpColor, 3-stop SVG gradient always applied; track keeps lap classes). **`--score-neg-1` recolored #fca5a5 → #f87171** (light red, not light pink). **CSV import shows a real progress modal** (`#import-progress-modal`, per-row bar + n/total + skipped count, not dismissable mid-run, renderer yields every 5 rows). **Logged combos gained Duplicate** in their breakdown actions (Close / Duplicate / Edit Combo / Delete All — re-logs every ingredient same day/amounts via `onDuplicateCombo`). **Logs select-mode gained an All button** (force-selects every loaded entry: foods + waters + combo members, `onSelectAll`). Pass-21 content (builder smoothness + main.ts 3,203→391-line decomposition into `src/ui/features/*`) is detailed below and still awaiting ONE commit go-ahead covering both passes.

---

## 1. Project at a glance

Local-first nutrition tracker (Android APK via Capacitor + web preview). Source of truth: **`Ai Guidelines/`** (master spec, governance rules, AI logs) + **`PLAN.md`** + the actual code.

```
UI events → services (food/ai/import/export/backup/transfer) → domain logic → repositories → SQLite
```

Core principle: **Gemma interprets. Code calculates. SQLite remembers.**

---

## 2. What is implemented and working

- SQLite schema v001 (12 tables) + migrations + repositories + fallback connection
- Deterministic domain: nutrition scaling, hydration gating, goals, scoring (unchanged, per spec)
- UI: dashboard (ring, macros, hydration breakdown, 1-tap recents), history (trend chart, heatmap, day list), goals view, all modals
- Services: CSV export, CSV import, Supabase migration, backup archive, GemmaClient (native bridge + deterministic fallback parsers)
- Native: Capacitor Android project, `GemmaPlugin.kt` (MediaPipe LLM), `VisionPlugin.kt` (ML Kit barcode + label OCR, pass 13)
- **Pass 2 additions:** FoodService pipeline, text logging, journal search + quick-log, action hub (edit/duplicate/delete), edit modal with amount multiplier, numpad water, CSV import wiring, backup wiring, barcode string lookup, 28-day historical score computation, vitest test suite
- **Pass 3 additions:** restore-from-backup (service `restoreBackupArchive` + Settings UI wiring, fallback-store replacement path, atomic via transaction when available, FK-safe clear/insert order), CSV import now handles quoted fields with commas, 48 new regression tests (backup/restore, Gemma fallback parsers, CSV import/export, barcode/alias/food repositories) — suite now 82 tests
- **Pass 4 additions:** real-SQLite regression coverage using the existing `sql.js` dependency (no new deps, no production code touched):
  - `src/data/migrations/migration.test.ts` (7 tests) — the actual `v001__init.sql` runs on a real SQLite engine: all 12 tables + 5 indexes created, migration idempotency, FK enforcement (orphan food_logs rejected), NOT NULL/CHECK constraints (confidence range, water source enum), `schema_version` round-trip, byte-level export/reload persistence
  - `src/data/repositories/sqlite-real.test.ts` (27 tests) — a thin adapter wraps sql.js in the Capacitor `SQLiteDBConnection` surface, then exercises every repository against a REAL SQL engine: food insert/update/upsert/alias-JOIN/fuzzy-LIKE-with-bound-LIMIT, log insert/JOIN food_name/SUM totals/duplicate/update/delete, water GROUP BY by source, goal end-date closing + boundary resolution + overlap validation, daily records, observations, imports, barcode ON CONFLICT re-map + verified flag, combos (create/get/list/update/cascade delete, FK rejection), FK cascades (foods → aliases/barcodes) and the no-silent-history-loss rule (foods with logs can't be deleted), combo expansion round-trip (repo combo → domain `expandCombo` → logs → daily totals match), and backup/restore round-trip on real SQLite (full-table equality, complete replacement, transaction rollback on constraint failure leaves DB untouched, empty-archive restore)
  - `src/services/ai/gemma-client.test.ts` (+4 tests, 11 → 15) — label OCR edge cases: unit-suffixed values, "of which" sub-lines, missing macros stay 0, and a documented test locking the preserved old-app behavior for kJ-first EU energy lines
  - `src/types/sql-js.d.ts` — minimal ambient declarations for the untyped `sql.js` package (test-only)
- **Pass 5 additions:** **encrypted backup format (spec §23)** — the Backup button now really encrypts:
  - `src/services/backup/encryption.ts` (new) — password-based encryption envelope via the built-in **Web Crypto API** (zero new dependencies, fully local/offline): PBKDF2-HMAC-SHA256 (200,000 iterations, 16-byte random salt) → AES-256-GCM (12-byte random IV). Envelope is JSON carrying base64 salt/iv/ciphertext + KDF params, so the file is self-contained and portable. `encryptBackup(jsonText, password)` → envelope string (throws on empty/whitespace-only password); `decryptBackup(payload, password)` → original JSON or `null` (wrong password, tampering, or non-envelope); `isEncryptedBackup(text)` → cheap format sniff.
  - `src/main.ts` — backup handler now prompts for a password (set + confirm), encrypts, then downloads; restore handler detects an encrypted file, prompts for the password, decrypts, then runs the existing parse/validate/restore flow. **Plain JSON archives from previous passes remain fully restorable without a password** (backward compatible).
  - `src/services/backup/encryption.test.ts` (new, 17 tests) — envelope structure (fields, base64 lengths, iteration count), no plaintext leakage inside the envelope, fresh salt/IV per run (two runs differ), empty/whitespace password rejection, byte-for-byte round-trip (also at the real default 200k KDF cost), decrypted output validates as an EverydayFuel archive, wrong password → null, missing envelope fields → null, plain JSON/garbage → null, AES-GCM authentication failure on tampered ciphertext → null, tampered salt → null, foreign format → null, and `isEncryptedBackup` true/false cases.
- **Pass 6 additions:** **scoring now reproduces the old app's full output contract (spec §18 + §29)**:
  - `src/domain/scoring.ts` — the numeric algorithm is untouched (score, scoreCode, components, clamping all identical), but `result`, `scoreTier` and `reason` now reproduce `old_app/app.js` `exportDataToCSV` byte-for-byte: `result` is the visual state (`Green`/`Grey`/`Red`), `scoreTier` is the legacy CSS class (`score-pos-5` … `score-0` … `score-neg-3` — replaces the invented word labels 'perfect'/'excellent'/…), and `reason` uses the legacy templated sentences ("Flawless day. Nailed everything: …", "Off target across the board (…)", etc.). `getScoreTier` (the invented word labels) was removed — nothing referenced it except `calculateScore` itself; the UI uses `getScoreColorClass`, which is unchanged.
  - `src/domain/scoring-regression.test.ts` (new, 3 tests) — replays **95 of the 96 rows of the supplied legacy export `exportexample.csv`** (the regression reference from spec §21/§29) through the new `calculateScore` and asserts byte-for-byte equality of `score`, `scoreCode`, `scoreTier`, `result` and `reason` against the old app's actual output. The single non-replayable row (`2026-05-03`) is asserted and documented: the legacy CSV records only "Pure Water (ml)" but the old app scored hydration against pure + drink/food water, so rows where the reason says "hydration goal met" while pure water is < 80% of target cannot be reconstructed from the CSV alone.
  - `src/domain/domain.test.ts` — updated `scoreTier` expectation ('perfect' → 'score-pos-5') and extended two `calculateScore` tests with legacy `result`/`reason` assertions.
  - `src/data/repositories/sqlite-real.test.ts` — fixed a **pre-existing flaky test**: the import-history test inserted two rows in the same millisecond, making `ORDER BY imported_at DESC` ambiguous in real SQLite (occasionally failed with 'csv' before 'supabase'). Now uses `vi.useFakeTimers()` with distinct system times so the ordering assertion is deterministic.
- **Pass 7 additions:** Phase 7 JS-side groundwork + WebView-safe dialogs (no Java/Android SDK in this environment, so native Kotlin/ML Kit work was NOT attempted):
  - `src/services/food/food-service.ts` — new `FoodService.logLabelOcr(date, ocr, amountG=100)`: full label-OCR input path through the established pipeline. Resolves the product (normalized → stripped → alias → new `nutrition_label` library entry with per-100g values + OCR confidence), records a `food_observations` row (`source_type: 'label_ocr'`, raw text + interpretation JSON for provenance), calculates nutrition deterministically, inserts the food log, and stores food-derived water separately (`classifyWaterSource`). Validation: throws on missing food name or non-positive amount.
  - `src/index.html` — scanner modal gains the label-text fallback UI (textarea `label-ocr-text`, amount `label-amount` default 100g, `btn-parse-label-text`); new `#password-modal` (title, password + confirm fields, inline error, Enter-key support) and `#confirm-modal` (message + destructive OK/Cancel).
  - `src/main.ts` — wired `btn-parse-label-text` → `gemmaClient.parseNutritionLabel` (the regression-locked deterministic parser) → `foodService.logLabelOcr` → refresh + toast; the `ai-file-input` change handler now resets the input and toasts the paste-text fallback; **all four `window.prompt`/`window.confirm` call sites (backup ×2, restore ×2) replaced** with promise-based `requestPassword(title, requireConfirm)` / `requestConfirmation(title, message)` in-app modals — the pass-5 flagged follow-up for Android WebView. Backup: 'Set Backup Password' (confirm required, empty/mismatch validated in-modal); restore: 'Enter Backup Password' for encrypted archives + confirm modal before replacing data. Cancel aborts exactly like the old null path; plain JSON archives still restore without a password.
  - `src/services/food/food-service.test.ts` — +3 tests (new `FoodService label OCR pipeline` block): unknown product → `nutrition_label` food + `label_ocr` observation + log with scaled macros (165 kcal/100g × 250g = 412.5); food-derived water logged separately with `food_log_id` link and library reuse on re-scan; validation errors. Suite now 143 tests.
- **Pass 8 additions: P2P transfer, Phase 9 (spec §23) — JS-side verifiable slice + full Settings UI flow** (no Java/Android SDK needed; the only untestable-here part is the WebRTC adapter itself, explicitly flagged):
  - `src/services/transfer/protocol.ts` (new) — pure protocol logic, no WebRTC/DOM deps: pairing codes (`encodePairingCode`/`parsePairingCode` — compact self-describing JSON, foreign apps/wrong versions/empty SDP → null), `TransferMessage` union + strict `parseTransferMessage` (garbage/unknown/malformed → null, untrusted-peer discipline), `chunkPayload`/`reassembleChunks` (16 KiB chunks), `sha256Hex` (Web Crypto), app tag/protocol version/256 MB payload guard constants.
  - `src/services/transfer/transport.ts` (new) — `TransferPeer` (send/onMessage/onOpen/onClose/close) + `TransferTransport` (receiver `createPairingCode()` → sender `connect(code)` → receiver `acceptConnection(answer)`) — the seam that makes the whole protocol testable in Node.
  - `src/services/transfer/transfer.ts` (new) — `P2PTransferService.sendBackup` (waits for open, streams `hello → meta → data chunks → done`, resolves on `ok`) and `receiveBackup` (validates hello/meta/chunk order, reassembles, verifies size + SHA-256, sends `ok`/`error`, rejects on close/violation/timeout). Timeouts + chunk size configurable for tests.
  - `src/services/transfer/webrtc-transport.ts` (new) — `WebRTCTransport`: thin `RTCPeerConnection`/`RTCDataChannel` adapter, no ICE servers (LAN/hotspot host candidates, no cloud), sends queue until the channel opens, `dispose()` releases. **Not exercised in Node (no `RTCPeerConnection`) — needs a browser/device pass.**
  - `src/services/backup/backup.ts` — extracted **`collectAllTables(db)`** (all 12 tables → `Record<string, any[]>`); the file-backup handler now uses it, so file backup and P2P send share one archive path.
  - `src/index.html` + `src/main.ts` — Settings gains **Receive/Send Backup (P2P Transfer)** buttons + `#p2p-modal` (pairing code out/in textareas, Copy Code, Connect, status, Cancel). Two-step manual pairing: *Receive* shows its offer code → pastes the sender's answer → Connect → password prompt → `decryptBackup` → parse/validate → in-app confirm → existing `restoreBackupArchive`/fallback-store restore; *Send* pastes the receiver's code → Connect → shows its answer code → password prompt (set + confirm) → `collectAllTables` + `createBackupArchive` + `encryptBackup` → `sendBackup` with percentage progress. Reuses the exact backup/encryption/restore logic as file backup.
  - Tests: `src/services/transfer/transfer.test.ts` (new, 19 tests, stub transport: encrypted round trip byte-for-byte + decrypt/validate on receive, multi-chunk streaming, checksum mismatch, foreign hello, close-mid-transfer, error propagation to sender, empty/oversized guards both sides, pairing/message parse rejects, chunking edges, SHA-256 known vector, channel-open timeout via fake timers) + `collectAllTables` real-SQLite test. Suite now **163 tests, 12 files**.
- **Pass 9 additions: Phase 10 — laptop view (spec §25) + Phase 11 static offline audit**:
  - `src/style.css` — the existing `@media (min-width: 768px)` block previously only laid out the dashboard. Now the **History view** becomes a 2-column grid on laptop screens (tabs card + trend chart span full width; consistency heatmap and the selected-day log list sit side by side; `#day-view-container` renders as a proper card; trend chart gets a taller 140px reading area), and the **Goals view** becomes a 2-column grid (`.view-header` title spans both columns; Active Goals Configuration and Data & Backup Tools cards sit side by side — the second card's inline `margin-top: 16px` is neutralized on desktop only since the grid gap owns spacing). Mobile (<768px) CSS is byte-for-byte untouched; rules are scoped `#history.active-view` / `#view-goals.active-view` so the ID-level `display: grid` never overrides `.view { display: none }` on hidden views. No HTML/TS/data changes.
  - **Offline audit (no code change):** grepped all of `src/` for network usage — the only touchpoint in the whole app is the Google Fonts `<link>` in `index.html` (pre-existing, same as old app, `display=swap` → graceful sans-serif fallback offline). No `fetch`/`XHR`/`WebSocket`/`axios` anywhere; SQLite, Web Crypto, and P2P WebRTC (host-candidate ICE) are all local. Kept deliberately; documented for the Phase 11 device pass.
- **Pass 10 additions — history UX rewrite (restores legacy app behavior), dedicated text log, shim fixes, P2P STUN** (all four reported-bug areas from the user's pass-9 feedback):
  - `src/services/history/history-window.ts` (new) — the History window computation (spec §20: derived on demand, never stored as a second dataset): `computeHistoryWindow(dates, repos)` replays the dashboard's deterministic pipeline per day (goal-for-date → targets, daily totals, water by source, hydration gating, score) and returns `hasData` per day (the legacy app rendered days with no logs as empty blocks — old_app/app.js `score-empty` — so the heatmap/chart can skip them instead of showing neutral grey). Also exports `datesForRange` / `datesForMonth` / `datesForYear` (leap-year aware) and null-safe `mapGoalToTargets`.
  - `src/ui/views/history.ts` (rewritten) — renderHistory now takes a window (days + view + anchor + selectedDate): **navigation header with ◀ ▶ paging**, legacy-style heatmaps — week (7 blocks), month (calendar grid with S M T W T F S headers + leading blanks), year (12 month-blocks of tiny day dots, 3-column grid) — **empty days rendered `score-empty` (invisible), not score-0 grey**; the trend chart shows calorie-ratio bars (`cal/goal`) per data day for week/month and a stats row (days tracked, avg kcal, total hydration) for year; clicking a block dispatches `select-history-date` and only moves the highlight + detail list — **selection is independent of the window** (fixes "calendar stuck on bottom-right day").
  - `src/main.ts` — replaced the 28-day `scoresByDate`/`ensureScoresForDate` machinery with `ensureHistoryWindow()` (window keyed by `view|anchor`, recomputed only when those change) + `renderHistoryView()` (re-renders from the cached window after every state refresh when the History tab is active); wired the previously dead **7D/1M/1Y tab buttons** (`setHistoryView`) and `history-nav` ◀ ▶ paging (`shiftHistoryAnchor`: ±7 days / ±1 month / ±1 year); restore + P2P-restore now invalidate the window key instead of `lastComputedScoreDate`.
  - **Dedicated Text Log (separate from the journal):** `src/index.html` gains a `+ TEXT` button on the Recents card and a `#text-log-modal` (input + "Interpret & Log"); `setupTextLogHandlers()` opens/closes it, Enter logs, and `logTextInput` now targets this modal. The journal (`+ JOURNAL`) is now **library-only**: Enter always fuzzy-searches (the old auto-detected text-log branch `isTextLogInput` was removed), and the empty-state hint points to +TEXT. Modal title changed to "Food Library".
  - **Fallback shim fixes** (`src/data/database.ts`): the evaluator was extracted into an exported, directly testable `createFallbackConnection(store)` factory (DatabaseManager wraps its fallback store in a `FallbackTableStore` adapter). Three real bugs fixed: (1) **INSERTs with positional `VALUES (?, …)` never wrote data** — values were keyed by value-position instead of column (broke all inserts in fallback mode, including goals — the reported "goal config not saving"); (2) `COUNT(*)` aggregation missing (goal overlap validation returned `undefined !== 0` → saves could be rejected); (3) `LIMIT` without `ORDER BY` ignored (fuzzy search ignored its bound). Also handles `INSERT OR REPLACE`, and `getWaterTotalsBySource`'s `GROUP BY source` was already simulated. UPDATE by id/date, ORDER BY/LIMIT, ON CONFLICT + `excluded.*`, DELETE by id/combo_id, and the `JOIN foods` name enrichment were implemented in pass 10 (earlier in this pass) and are now regression-covered.
  - `src/services/transfer/webrtc-transport.ts` — **configurable ICE servers** (`WebRTCTransport(iceServers = DEFAULT_ICE_SERVERS)`): defaults to two public STUN servers (Google + Cloudflare) to fix "WebRTC: ICE failed" pairing failures (mDNS/`.local` host-candidate resolution, NAT mapping). No TURN by design — payloads are never relayed through third parties; host candidates still connect directly on a plain LAN/hotspot. **Pass-10 follow-up (user-reported):** `waitForIceGatheringComplete` no longer blocks on unreachable STUN (resolves on first candidates + 2s grace, hard cap 15s — the "Timed out gathering connection candidates" failure) and the P2P modal gained an **optional TURN server config** (URL + user + pass, persisted in localStorage `everydayfuel_p2p_turn`) for pairing across different networks.
  - **QR-code pairing (spec §23 "shown as text/QR"):** `src/services/transfer/qr-code.ts` (new) — `renderPairingCodeAsQR` (tiny `qrcode` package, ECC L for large SDP payloads, rejects oversized payloads), `supportsQrScanning` + `startQrScan` (native `BarcodeDetector` API, zero deps; every-4th-frame decode loop; graceful rejection when unsupported). The P2P modal now shows the receiver's code as a **big QR image** (text copy collapsed under a details toggle) and the sender gets a **"Scan QR Code"** button → camera modal → auto-fills → auto-connects (`onP2PScan`). Manual paste remains as the fallback everywhere; the scan button hides automatically where `BarcodeDetector` is missing. `AndroidManifest.xml` gained the `CAMERA` permission (WebView `getUserMedia`). One new runtime dep: `qrcode` (+ `@types/qrcode` dev). Tests: `qr-code.test.ts` (5 tests: data-URL render, large-payload render, oversized rejection, no-browser support detection, graceful scan failure).
  - `src/index.html` + `src/style.css` — `#text-log-modal`; `.history-nav` (paging header), `.heatmap-year` (3-col), `.month-block`/`.month-title`/`.month-days` (7-col day grid), `.day-dot` + `.day-dot.selected` (tiny year heatmap cells).
  - Tests: `src/data/database-shim.test.ts` (new, 12 tests — positional INSERT with literals/bound values, UPDATE by id/date, ORDER BY+LIMIT and standalone LIMIT, water GROUP BY by source, SUM(calories) totals, COUNT(*), current-goal + active-on-date resolution, JOIN-foods enrichment, ON CONFLICT barcode upsert with `excluded.*`, DELETE by id/combo_id, alias JOIN) and `src/services/history/history-window.test.ts` (new, 12 tests — range/month/year date builders incl. leap years, `dayHasData`, `mapGoalToTargets` defaults/null-safety, per-day score/totals/hydration computation, hydration gating, water-only days). Suite now **192 tests, 15 files**.
- **Pass 11 additions — full CSV export per spec §21 / QoL #9/#10 (all-time, date-range, goal-phase) with real metadata**:
  - `src/services/export/export-service.ts` (new) — `datesBetween(start, end)` (inclusive, month-boundary safe), `resolveExportDateRange(repos)` (all-time bounds: earliest of first food/water log + oldest goal start → latest of last food/water log + today), `goalPhaseRange(goal)` (goal span; `end_date` NULL → today), and `buildExportRows(dates, repos)` which replays the dashboard's deterministic pipeline per day (goal-for-date → targets, daily totals, water by source, hydration gating, full legacy score contract) plus daily-record metadata. **Days with no food/water data are skipped** (legacy behavior — old_app/app.js `exportDataToCSV` only emitted rows for days with entries; the regression reference `exportexample.csv` contains only data days). Emits the established 21-column `ExportRow` shape — the locked CSV format (csv-export.test.ts) is byte-for-byte unchanged.
  - Repo additions: `LogRepository.getFirstAndLastDate()` / `WaterRepository.getFirstAndLastDate()` (`SELECT MIN(date)/MAX(date)`) — used only by the all-time export.
  - `src/index.html` + `src/main.ts` — `#export-modal` (radio scope: Selected Day / All Time / Date Range / Goal Phase; From/To date inputs for range; goal-phase dropdown populated from `getGoalsHistory()`); `setupExportHandlers()` + `openExportModal()`; `btn-export-csv` now opens the modal. The previous handler hardcoded `goalName: 'Active Phase'`, `lowAccuracy: false`, `dailyNote: ''` for a single selected day — the modal's "Selected Day" scope preserves that behavior with **real** data (actual goal name, real low-accuracy flag + day note from `daily_records`).
  - `src/data/database.ts` — fallback shim now handles `SELECT MIN(date)/MAX(date)` (returned raw rows before → silently wrong all-time bounds in fallback mode).
  - Tests: `src/services/export/export-service.test.ts` (new, 12 tests against a **real SQLite engine** — same sql.js harness as sqlite-real.test.ts): date list building incl. month boundaries, all-time bounds (data + no-data fallback to today), goal-phase span with open end, one row per data day with the correct per-phase goal name/targets across a Cut→Bulk range (no relabeling of history), actuals + water breakdown + hydration gating (effective = explicit only below target; explicit + food once target reached), legacy score contract, low-accuracy flag + day note propagation, empty-day skipping + empty range, 21-column CSV round-trip. Plus 1 new shim MIN/MAX test. Suite now **205 tests, 16 files**.
- **Pass 12 additions — CSV import bug fix for the old app's export format** (user-reported "it imported wrong" when importing `exportexample.csv`):
  - **The bug:** `parseCSV` mapped columns by first substring match, so in the legacy 16-column daily-aggregate file the import read **`Target Kcal`/`Target Pro(g)`/`Target Carb(g)`/`Target Fat(g)` as if they were actuals** (every day imported the goal's 2500/150/295/80 instead of the real totals), ignored the `Pure Water (ml)` column entirely, and (no `Food` column being present) named every row `Imported Item`. "Imported wrong" indeed.
  - **The fix** (`src/services/import/csv-import.ts`): header resolution is now **key-priority** — each field tries its actual/total key before the generic one (`total cal` → `cal`, `total pro` → `pro`, `total carb` → `carb`, `total fat` → `fat`, `pure water` → `water`), so Target columns are never picked over Total/actual columns. The old app's `Logs` column is used as the food-name source (first food of the day's list, `"A | B | C"` → `A`) when no `Food`/`Name`/`Item` column exists, and `Pure Water (ml)` now flows into `ParsedImportRow.waterMl` — `main.ts` already inserted an explicit water log for `waterMl > 0`, so water imports correctly with zero main.ts changes.
  - **How a legacy day imports now (honest mapping, no fabrication):** one food log per day named after the day's first logged food, carrying that day's **actual totals** at 100 g (per-100g basis = totals, log = totals), plus the day's explicit water as a linked water log. Per-food breakdowns don't exist in the legacy format, so they are not invented. Tracker-style per-item CSVs (Date/Food/Calories/…) behave exactly as before (existing tests untouched, all still green).
  - Tests (+5, in `csv-import.test.ts`): legacy fixture asserts **actuals not targets** (1282 kcal / 65 pro / 181 carb / 30 fat — before the fix this was 2500/150/295/80), `waterMl` = Pure Water (0 and 2000 in the two fixture rows), food name from `Logs` (first segment), and a **regression block that reads the real `exportexample.csv` file** (via `readFileSync` from the repo root): all 96 days parse with zero errors, every row has a real food name, and the 2026-04-05 / 2026-04-06 rows assert the true totals (1282/65/181/30/water 0 and 1855/72/248/66/water 2000). Suite now **210 tests, 16 files**.
- **Pass 13 additions — Phase 7 native ML Kit (barcode + label OCR) + first working APK build** (pass-13 changes are uncommitted, see §6):
  - `android/app/src/main/java/com/everydayfuel/app/VisionPlugin.kt` (new) — Capacitor plugin with `scanBarcode({ imagePath | imageBase64 })` and `ocrLabel({ imagePath | imageBase64 })` on the already-declared ML Kit deps (`barcode-scanning:17.2.0` bundled, `text-recognition:16.0.0` Latin bundled — fully offline, no Play services). Images downscaled to 1280 px; `scanBarcode` resolves `{ barcode, format }` (null when none found), `ocrLabel` resolves `{ text }` raw OCR. Interpretation stays in the web layer (Gemma/deterministic → validation → food reference → SQLite, spec §7.3). Registered in `MainActivity`.
  - `src/services/vision/vision-client.ts` (new) — JS bridge: `getVisionPlugin()` (null in a plain browser), `stripDataUrlPrefix()`, `FOOD_BARCODE_FORMATS` (1D retail codes + QR), `supportsBrowserBarcodeScan()` / `startBrowserBarcodeScan()` (live camera scan via the built-in `BarcodeDetector`, same zero-dep approach as the pass-10 QR scan).
  - `src/index.html` — scanner modal gains `btn-scan-barcode` ("Scan Barcode (Camera)") + `barcode-scan-container` live-feed div.
  - `src/main.ts` — scanner flow rewired with zero behavior change to existing paths: `logBarcodeFood(code)` and `logLabelOcrText(text, amount)` extracted as shared helpers; `btn-scan-barcode` → native camera (`@capacitor/camera` Uri) → `scanBarcode` → log, or browser BarcodeDetector live feed (stops on detect/close), or toast + focus manual input; `btn-decode-label` → native camera → `ocrLabel` → existing interpret pipeline, browser keeps the pre-existing file-input path; `ai-file-input` change → native FileReader base64 → `ocrLabel`, browser keeps the "paste the label text instead" toast.
  - **Toolchain fixes required for the first-ever APK build:** `android/build.gradle` + `android/app/build.gradle` gain the `kotlin-gradle-plugin:2.2.20` classpath and `apply plugin: 'kotlin-android'` — the app module previously had NO Kotlin plugin, so `GemmaPlugin.kt` had never compiled (the APK could not have worked). `:app:compileDebugKotlin` now compiles GemmaPlugin.kt + VisionPlugin.kt cleanly. **JDK 21 installed at `C:\Android\jdk-21\home`** (Microsoft OpenJDK, downloaded this pass — `@capacitor/camera` hard-requires `jvmToolchain(21)`); use `$env:JAVA_HOME = "C:\Android\jdk-21\home"` before gradle invocations, no global change.
  - Tests: `src/services/vision/vision-client.test.ts` (new, 8 tests: plugin detection, data-URL stripping, format list, Node-side unsupported detection + graceful rejection). Suite now **218 tests, 17 files**.
- **Pass 14 additions — on-device verification (items 2–4 of §5, all DONE on the user's phone) + the spec §7.4 online barcode fallback** (pass-14 changes uncommitted, see §6):
  - `src/services/barcode/online-lookup.ts` (new) — `lookupBarcodeOnline(barcode, opts)` against the public **Open Food Facts** API (zero new deps: WebView `fetch` + AbortController, 6 s timeout): null on offline/timeout/non-OK/invalid JSON/product-not-found/no-name/**no-nutrition-data** (a found-but-worthless record still falls through); `energy-kcal_100g` preferred, kJ-only `energy_100g` ÷ 4.184; returns `OnlineBarcodeProduct { productName, calories/protein/carbs/fat per 100 g }`.
  - `src/services/food/food-service.ts` — `FoodService.logBarcodeLookup(date, product, barcode, amountG=100)` mirrors `logLabelOcr`: library reuse else new food with `source_type: 'barcode'` + `source_reference: <barcode>` (confidence 0.8), barcode observation, deterministic nutrition, food log, water handling.
  - `src/main.ts` — `logBarcodeFood` now: local hit → log (unchanged); **miss → "checking online…" → Open Food Facts → log + `barcodeRepo.saveBarcode(foodId, code, 'online')` (verified stays 0) → toast; offline/not-found → toast + scan-the-nutrition-label fallback (extracted `scanLabelFromScanner()`/`triggerLabelScanFallback()` — native camera/gallery OCR, web file picker), replacing the old "open manual log modal" fallback** (manual entry stays reachable via + MANUAL). `capturePhotoUri()` source `Camera` → `Prompt` (saved-photo OCR/barcode paths were dead code in the APK; verified with the system chooser).
  - Tests: `online-lookup.test.ts` (new, 13 tests) + `food-service.test.ts` (+3 barcode-pipeline tests). Suite now **234 tests, 18 files**.
  - **On-device verification (all ✓ on OPPO CPH2363):** online lookup of `3017620422003` (Nutella) → real OFF fetch → "Logged "Nutella" · 539 kcal", dashboard 1,095 → 1,634, recents updated, `foods` row (source_type `barcode`, source_reference) + `food_barcodes` row (source `online`, verified 0) in native SQLite; **airplane-mode rescan of the same barcode → instant local hit (1,634 → 2,173) — the online-saved product works fully offline**; unknown barcode offline → "Barcode not found online — scan the nutrition label instead" + camera chooser opened (label-scan fallback, not manual entry; cancel clean); gallery→OCR label flow works end-to-end via the Prompt chooser (user-run: existing library entry reused, 765 → 1,095 kcal); history 7D/1M/1Y + paging + stats; offline water/text logging; logcat clean.

- **Pass 15 additions — §5a dashboard layout items 1–4 + product naming 7 + grams panel 8, on-device verified** (pass-14 + pass-15 changes uncommitted, see §6):
  - **1-Tap Recents removed from the UI (§5a #1)** — `recents` dropped from `AppState`/`defaultState` (`src/ui/state.ts`) and the recents rendering removed (`src/ui/views/dashboard.ts`); the query is kept as a debug hook: `console.debug('[debug] recents (UI hidden):', …)` in `refreshStateForDate` — agent-visible via WebView CDP console only, never in the UI. `quick-log-recent` listener removed.
  - **Always-visible text bar (§5a #2)** — recents card replaced by `.textbar-card` with `#dash-text-input` + `#btn-dash-text-log`; `setupDashboardTextBar()` (replaces `setupTextLogHandlers()`), Enter/LOG → existing `logTextInput` → clears the bar; `#text-log-modal` deleted; journal empty-state copy points to the text bar.
  - **Journal + Manual top-left (§5a #3)** — `.sys-btn-left` fixed bar (`#btn-open-journal`, `#btn-open-manual`) opposite top-right Settings; desktop ≥768px: `top: 30px; left: 100px`.
  - **Vertical compression (§5a #4)** — main padding-top `max(52px, calc(env(safe-area-inset-top,0px)+52px))` (buttons below the 107px status bar, ring card 52px, no overlap — measured), view gap 16→12, card padding 16→12, grid gap 20→14 + bottom 16, progress ring 200→160, margin trims. Dashboard fits one 800dp screen, zero scroll.
  - **Label product naming (§5a #7)** — `gemma-client.ts`: exported `DEFAULT_LABEL_PRODUCT_NAME = 'Scanned Label Product'`; fallback label parser extracts a real product name from the OCR heading / keyword lines (sanitized, 100-char cap). `logLabelOcrText(text, amount, askGrams=true)`: placeholder or empty name → **ask the user** via the new `#name-modal` (`requestName`); barcode path never asks (online lookup provides the name).
  - **Grams panel for both scans (§5a #8)** — `requestGrams` + `#grams-modal` (title, per-100g subtitle, default 100, inline error, OK/Cancel, Enter); `logBarcodeFood` + `logBarcodeViaOnlineLookup` prompt for grams before logging — per-100g from the record, **grams = what the user ate** (old_app parity); Cancel aborts cleanly; paste-label dev path keeps `askGrams=false`.
  - **On-device verification (all ✓ on OPPO CPH2363):** text bar "100g banana" → 0 → 200 kcal, bar cleared; Nutella barcode → grams modal (default 100) → 50 g → logged at `amount_g: 50` = 269.5 kcal (539 × 0.5) → 470; unnamed label text → name modal → "Test Granola" → logged 250 kcal → 720; grams Cancel → no log, scanner stays open; CDP watcher confirmed the `[debug] recents (UI hidden): …` hook on every refresh; native SQLite rows verified (correct amounts/scaling); layout metrics: no overlap, single-screen dashboard.
  - Tests: `gemma-client.test.ts` (+5 name-extraction tests). Suite now **239 tests, 18 files**.

- **Pass 16 additions — layout polish + History perf + auto-update (user follow-up review)** — on-device verified on OPPO CPH2363:
  - **`viewport-fit=cover` + `env(safe-area-inset-top)`** (`src/index.html:5` viewport, `src/style.css:18` `main` `padding-top: max(64px, calc(env+64px))`, `.sys-btn-left/.sys-btn-top: max(32px, calc(env+28px))`) — whole UI pushed down; measured `paddingTop 100px` / `sysTop 64px` (was 20px under the 107px status bar), `env` was 36 on this device.
  - **Scan button into dock** (`src/style.css:scan-wrapper 70px flex center`, `#scan-btn:relative`) — measured dock `730-800` (70h), scan `733.5-797.5` (64h) now inset 3.5px below dock top (was `692-756`, 38dp float).
  - **History view-switch: batch range queries** — new repos `LogRepository.getDailyTotalsForRange` / `WaterRepository.getWaterTotalsBySourceForRange` / `GoalRepository.getGoalsForRange` (`src/data/repositories/*`, `GROUP BY date` + `GROUP BY date,source`, goal overlap `start<=to AND end>=from`), fallback shim branches `src/data/database.ts:329`, rewritten `computeHistoryWindow` (`src/services/history/history-window.ts:57` `Promise.all` 3 calls) + `HistoryRepos` interface update — on-device: week **104→21 ms**, month **733→21 ms**, year **8204→25 ms** (1095 → 3 native calls); year now shows 12 `month-block`s of `day-dot`s (was wedging CDP).
  - **Auto-updating heatmap cells** — `src/main.ts:60` `dataVersion`, `refreshStateForDate:59` `dataVersion++`, `ensureHistoryWindow:211` key `view|anchor|dataVersion` + `if(active) ensureHistoryWindow().then(render)` (was stale `view|anchor` + bare `render`).
  - Tests: `history-window.test.ts` fakes now batch-shaped +1 batch-path spy; `database-shim.test.ts` +2 grouped; `sqlite-real.test.ts` +3 batch. Suite now **245 tests, 18 files**.

- **Pass 17 additions — dashboard polish round 2 (user screenshot review)** — on-device verified on OPPO CPH2363:
  - **Scan button floats above the dock** (`src/style.css` `.scan-btn top: -7px`, desktop `top: auto`) — measured dock `730–800`, scan `726.5–790.5`: the circle breaks the dock's top edge by 3.5 px (was inset 3.5 px below it).
  - **Whole UI down 12 px** (`src/style.css:42` main padding-top `max(76px, env+76px)`, `.sys-btn-top` `max(44px, env+40px)`) — measured `paddingTop 112px` / `sysTop 76`; gear-to-content relationship preserved.
  - **Contextual LOG button** (`src/index.html` textbar card, `src/style.css` `.text-bar.has-text button`, `src/main.ts` `setupDashboardTextBar` input listener + `logTextInput` class clear) — hidden until non-whitespace text is typed; Enter submit unchanged; hides again after a successful log.
  - **Journal/Manual integrated** (`src/index.html` `.textbar-utils` row inside the textbar card, same IDs `btn-open-journal`/`btn-open-manual`; floating `.sys-btn-left` pills + CSS deleted) — modal handlers in `main.ts` untouched. Now dashboard-only by design (user request); not reachable from Logs/Goals views.
  - Verification: build/tests/sync/APK/install ✅; CDP layout numbers above; functional click-through (type → LOG appears → log lands → button hides → input clears), test entry deleted afterwards via action hub (data left as found); Journal/Manual modals open/close from the new row. Final visual screenshot ❌ (phone locked at that moment — CDP numbers stand in).

- **Pass 18 additions — §5b rework + water deletion + three real bug fixes (user authorized mid-pass)** — on-device verified on OPPO CPH2363:
  - **Inline log expansion (§5b-1)** — new `src/ui/views/day-detail.ts` renders the selected-day list: food logs expand inline with Edit/Duplicate/Delete; water entries listed with inline Delete (new user request); log notes render on items (§5b-8); day-note row on top (§5b-5). Action-hub modal deleted. Day notes write via existing `dailyRecordRepo.setNote`; multi-select (§5b-6) has Change Date/Duplicate/Delete only, water excluded from move/dup; combos UI (§5b-7) in the Food Library ("+ Combo" on results → name → save; Log All runs domain `expandCombo` → deterministic nutrition + water handling).
  - **Navigation rework (§5b-2/3/4)** — new `src/ui/nav.ts`: History-API layer stack (every tab switch/modal/edit pushes one entry; BACK pops LIFO in-app; prompt promises settle on BACK-as-cancel), full-screen `#view-edit`, swipe between tabs (|dx|≥70 and ≥1.6·|dy|, suppressed with modals open). *(Superseded in pass 19: @capacitor/app now intercepts the gesture itself — see below.)*
  - **Export fixes:** `datesBetween` rewritten pure-UTC — DST was dropping the final range day (Africa/Cairo springs forward AT midnight 2026-04-24; verified both on device and by regression test); `buildExportRows` now batched (3 range queries vs ~400 sequential calls) with probe-guarded per-date shim fallback; all-time export went 111→112 days with today included.
  - **Native downloads:** new `SaveFilePlugin.kt` (+ MainActivity registration, JS bridge `src/services/native/file-saver.ts`) saves CSV/backups into public `/sdcard/Download` via MediaStore (no permission API ≥29, same-name overwrite); `downloadCSV`/`downloadBackup` use it only when native; browsers unchanged.
  - **Restore fix:** jeep-web/WASM doesn't hold BEGIN across run() calls — commit failure "no transaction is active" after successful writes is now tolerated (warned, non-atomic there); anything else rethrows; native transactions untouched.
  - On-device verification: export modal all four scopes clicked through with content checks; inline expand/collapse, edit-screen open/save/back; water add+delete round-trip incl. offline; day-note set/clear; multi-select dup(1→2)/delete(2→1); modal-BACK closes journal; synthetic swipe DASH→LOGS; combo create→log(+200 kcal)→cleanup; **P2P phone↔headless-Chrome end-to-end**; airplane-mode offline pass; final screenshot shows user's own day note "Bom" + log notes "Yauc".
  - Tests: `export-service.test.ts` +2 regression tests (DST-transition bounds; trailing water-only day in a 133-date range). Suite then **247 tests, 18 files**. Committed as `8b69c6d`.

- **Pass 19 additions — §5c implemented in full except item 1 (food-image pipeline) + item 2 (Gemma model, user-driven)** — on-device verified on OPPO CPH2363:
  - **Real full-screen edit view (§5c-A):** `#view-edit` moved INSIDE `<main>` (it previously sat outside the container with no safe-area padding — the reported ‹ Back/status-bar collision); `body.edit-open` hides `.dock` + `.sys-btn-top`; slide-up entrance animation. Measured on device: back button top = 112 px, dock/gear `display:none` while editing.
  - **Back-button root cause + fix (§5c-B/#7):** Capacitor 8 core registers NO back handling at all (verified in the packaged sources) — nothing intercepted hardware back or predictive-back, so the activity just finished. New dependency **@capacitor/app 8.1.1** (user pre-approved): its native `OnBackPressedCallback` handles both; when a JS `backButton` listener is registered (`initNativeBackButton()` in nav.ts) the event routes to the existing layer stack (LIFO pop); at the base layer `App.exitApp()` preserves standard exit. History-API stack kept for web/browser-back. Verified on device with the REAL `KEYCODE_BACK` keyevent AND a real edge swipe: both navigate in-app now.
  - **Transitions/animations (§5c-7):** direction-aware entrance animations for tab switches/swipes (`animateViewIn`/`tabDirection`, transform+opacity only, auto-cleaned), modal backdrop fade + content spring-in, edit-screen slide-up. Swipe gestures suppressed while editing or modals are open.
  - **Journal-style logs list (§5c-D):** `day-detail.ts` rewritten — day-group headers (weekday long-form + date/note subline + ⚠ low-accuracy chip + ✎ note button + running day-total kcal), entries chronological with local HH:MM time chips per item and kcal + P/C/F profile; groups newest-first, only days with entries; **Load Older Days** pages back by 14 days; selected-date group highlighted; multi-select spans all loaded days (combo rows select all member logs; water still excluded from move/dup); inline Edit/Duplicate/Delete preserved. Four batched range queries per render (new repo methods `getLogsForRange` w/ name JOIN, `getWaterForRange`, plus totals + daily records).
  - **Combos rebuilt (§5c-C):** creation fixed via an explicit builder mode — "+ New Combo" opens a builder panel (item chips × removable, name input, Save/Cancel); while building, tapping library results ADDS to the builder instead of quick-log-and-close-modal (the old flow destroyed the draft — why creation was unusable in practice, evidenced by 8 duplicate templates in the user's DB). Saved combos tap-expand into **NOURISHMENT BREAKDOWN** cards: per-ingredient amount/macros/kcal, total row, Close / Log All / Edit / Delete. Logged combos: `logCombo` creates ONE shared `food_observations` marker (`source_type:'combo'`, interpretation JSON `{kind,comboId,comboName}`) linked from every ingredient log's `observation_id` (no schema change); the journal collapses such clusters (≥2 logs sharing a combo observation — duplicates of single items are verified against the observation before clustering) into one expandable row with the same breakdown card + Close / Edit Combo / Delete All.
  - **Dashboard (§5b-10/11, §5c-E):** new `.day-header` above the ring card (date "Today/Yesterday/Fri, Aug 22" left, tier-colored day-score badge right, both driven from state in `dashboard.ts`); `.ring-card { margin-bottom:30px }` pull-up opening space above the macro bars.
  - **Low-accuracy flag (§5c-3):** ⚠ chip on every journal group header toggles `daily_records.low_accuracy` for that date (label expands to "⚠ Low Accuracy" when set); exports carry it as before.
  - **Day-note UX (§5c-F):** whole group header area editable via ✎ per group (modal prefills from that date's record; header subline shows "📝 note" live).
  - **Legacy CSV import split (§5c-4):** days from the old app's daily-aggregate format expand into ONE ROW PER FOOD parsed from the `Logs` cell; day totals split equally (exact division — sums stay byte-equal to the legacy day totals), water attaches to the first row only, split rows flagged `estimatedSplit` (library confidence 0.5) and their days get `low_accuracy=1`. Tracker-style CSVs unchanged.
  - **Confidence export columns (§5c-4):** `ExportRow` gains `avgConfidence`/`minConfidence` (per-day AVG/MIN of logged foods' confidence via new batched `LogRepository.getDailyConfidenceForRange`, shim branch included, degraded connections export empty cells); CSV header 21→**23** columns ('Avg Confidence','Min Confidence', 2-decimal or empty); locked-format tests updated.
  - **Goals save (§5c-12):** `btn-save-goals` now updates the ACTIVE goal in place (`GoalRepository.updateGoalTargets`) instead of closing it and creating a fresh phase per save; create path kept only when no goal exists.
  - **kJ-first OCR quirk (§5c-13):** label parser prefers the explicit `<n> kcal` value on the energy line; kJ-only lines convert ÷4.184 (same rule as online lookup); regression test updated (1542→368 / kJ-only→368.5).
  - **Local font (§5c-11):** Outfit variable font (300–900, latin + latin-ext woff2) bundled at `src/public/fonts/`; Google Fonts `<link>` removed; `@font-face` blocks in style.css. The app's last network touchpoint is gone.
  - **Cleanup (§5c-16):** dead `state.selectedLogForAction` removed. Also removed the old top-of-list day-note bar (superseded by per-group headers).
  - **Two device-found races fixed during verification:** concurrent async renders interleaved with `innerHTML=''` clears duplicated builder chips/combo rows; slower stale renders overwrote newer data (post-save list showed pre-save combos). Fixed with detached-fragment rendering + generation counters in `renderComboBuilder`/`renderComboList`/`renderJournalIfVisible`.
  - On-device verification: REAL hardware BACK closes edit screen (dock returns, app stays); REAL swipe-back navigates LOGS→DASH (the exact gesture that exited the app before); tab-swipe DASH↔LOGS with animation class observed; edit screen full-screen metrics; journal renders 13 day-groups with correct weekday/date/kcal/times/P-C-F/notes; combo builder create (chips, modal stays open) → save → Log All (+365 kcal toast) → journal cluster row → breakdown card → Delete All cleanup; library breakdown Close/Log All/Edit/Delete; low-accuracy toggle round-trip (DB 0→1→0); day-note set/render/clear (user's "Bom" untouched); goal save updates in place (same id, 2500→2550→2500 restored, still 1 row); Selected-Day export pulled from `/sdcard/Download`: 23 columns incl. Avg 0.84 / Min 0.7; Outfit loads locally (`document.fonts.check` true, airplane-safe); dashboard screenshot shows date+score header and ring gap. User-data integrity: all test artifacts deleted through the new UI; combos table back to the 8 pre-existing rows; goals 1 row @2500.
  - Tests: csv-import split regressions (+3 rewritten blocks), csv-export/export-service 23-column locks + confidence aggregation tests, sqlite-real +4 (range JOIN names, water range, confidence AVG/MIN, updateGoalTargets in-place), database-shim +2 (confidence GROUP BY branch, water range listing), gemma-client kJ fixes (+3/-1). Suite now **257 tests, 18 files**.

- **Pass 20 additions — §5d: Journal → INDEX screen rework + combo remake + dashboard/logs/water UX round** (NOT on-device verified this pass — user requested build+install only):
  - **Index screen (replaces the Food Library modal):** `#journal-modal` deleted; new `#view-index` tab (dock gains INDEX between DASH and the scan button; swipe order today↔index↔history via `TAB_ORDER` in nav.ts). Header (INDEX / Nourishment Library), rounded fuzzy-search bar (same `fuzzySearch`, semantic = future), FOODS|COMBOS segmented tabs, sort `<select>` per tab — foods: date created / kcal / protein / carbs / fat / confidence ×(high|low); combos: date / kcal / ingredients / name. Foods list shows name + per-100g kcal + P/C/F + a confidence % chip; rows expand inline ("Other amount…" goes through `requestGrams`). Combos list shows kcal + macro totals + ingredient count; tap opens the detail modal. `FoodRepository.getAllFoods` added.
  - **Combo detail modal (`#combo-detail-modal`, image-2 layout):** name + created-date top-left, big kcal top-right, colored macro line, divider, NOURISHMENT BREAKDOWN rows (amount · P/C/F left, kcal right) that expand inline to the per-100g profile + confidence + source, TOTALS row with all macros, primary Log All button + Close/Edit/Delete grid. Edit → full-screen builder; Delete confirms (logged meals untouched); Log All runs the existing deterministic combo path and closes the modal.
  - **Combo builder remade as a full-screen view (`#view-combo-builder`, body.builder-open hides dock/gear like edit-open):** name field + live totals card (kcal/P/C/F recompute on every amount change via cached food lookups), ADD INGREDIENTS search card (tap adds at 100 g, "✓ In Combo" badge), INGREDIENTS card with per-item editable gram amounts + remove buttons, Save persists through `createCombo/updateCombo` with real amounts (old builder hard-coded 100 g). Back gesture/stack handled by one pushed layer; drafts reset on close by design.
  - **Dashboard header:** actual date always ("Sat, Aug 23" — Today/Yesterday logic removed from `formatDashboardDate`); score is a bare number (+N/0/−N) tier-colored via `getScoreColorClass` (text + border) and tapping it opens the Daily Score modal: big colored number, legacy reason sentence, per-component ±1 breakdown rows (from `ScoreResult.components`), formula footnote.
  - **Logs isolation + ordering:** window defaults to ONLY the selected date (`journalDaysShown=1`; reset on every heatmap day switch); "Load Older Days" pages −14 days; header gains Newest ▾/Oldest ▴ toggle (`journalDayOrder`) sorting groups either way; delete-water now confirms like delete-food.
  - **Water rows = food-log UI:** tap expands inline with Duplicate (same amount/source/date) / Edit / Delete; Edit opens `#water-edit-modal` — amount + date fields ONLY — persisted via new `WaterRepository.updateWaterLog(id,{amount_ml?,date?})` (+ `getWaterById`); both dates refreshed when the entry moves days.
  - **Week view:** weekday initial letters above each square (per-date narrow weekday names, shared cell builder with month view).
  - **Settings:** "Import Tracker CSV" → "Import Data"; new Danger Zone card with Delete All Data → confirm modal → exported `wipeAllData(db)` (FK-safe CLEAR_ORDER over all 12 tables) or fallback-store replacement, history window invalidated, state + index re-rendered. Fallback shim gained the no-WHERE DELETE branch (was a silent no-op).
  - Nav internals: `ViewId` + TAB_ORDER include 'index'; `switchTabInternal` assigns `currentViewId` before data loads (renderIndex guards on it); openEditView hides views generically via querySelectorAll.
  - Tests: shim +2 (water UPDATE amount/date by id; no-WHERE DELETE wipes table), sqlite-real +3 (`getAllFoods` newest-first ordering, `updateWaterLog` amount/date-only + no-op safety, `wipeAllData` clears every backup table then stays usable). Suite now **262 tests, 18 files**.

- **Pass 20 follow-up — first user feedback round (4 fixes, installed via adb, no on-app testing per user):**
  - **Combo-save false error (user: "error everytime i save a combo but in the end it works"):** `ComboRepository.createCombo/updateCombo` now run through a shared `withTransaction` helper that tolerates connections which cannot hold a manual transaction across run() calls (benign "no transaction is active" COMMIT state warned + treated as committed — same contract as the pass-18 restore fix); ROLLBACK is best-effort. `saveComboFromBuilder` additionally verifies persistence against the DB when the save path throws before showing any error — a combo that actually landed never surfaces a failure toast.
  - **Dock:** INDEX kept in the dock; dock rebuilt as a grid (`grid-template-columns: 1fr 1fr 96px 1fr`) so the scan circle is EXACTLY centered at 50% width regardless of label widths; desktop ≥768px overrides back to the vertical flex rail (`display:flex`).
  - **Dashboard header:** score moved from the right edge to sit directly NEXT to the date (`.day-header` flex-start + 12px gap, baseline-aligned).
  - **Imports:** CSV import no longer auto-sets `daily_records.low_accuracy` on split/reconstructed days (the 0.5 library confidence on split rows remains as the estimate marker); existing flags untouched.

- **Pass 20 follow-up 2 — second user feedback round (installed via adb, no on-app testing):**
  - **Dock reverted to DASH · scan · LOGS** (user chose option B after two rounds: Index lives on the DASH view next to Manual; swipe today↔index↔history still works). Grid experiment removed.
  - **Score badge:** border removed entirely (bare tier-colored number), day-header lifted 8px off the ring card so nothing touches the circle.
  - **Gradient ring:** first-lap stroke is now a two-stop SVG linearGradient driven by how the day is going — deep red <50%, amber 50–85%, light→deep green in the 85–115% band, amber→red over 115% (`ring.ts` `ringGradientColors` + create-once `ensureRingGradient`); multi-lap over-target laps keep their class colors.
  - **Logs:** "Load Older Days" removed entirely (strictly the selected day; `journalDaysShown` paging machinery deleted, `DayDetailArgs` lost `hasMoreDays`/`onLoadMore`).

- **Pass 20 on-device verification — done by the USER (2026-08-24):** the user ran their own click-through of the §5d pass-20 build and reported it working ("i did the first one and tested it").

- **Pass 21 — combo-builder smoothness + main.ts decomposition (structural-risk fix from the general scan):** NOT yet on-device verified (build+test+headless-boot only, per the usual cadence — user tests before commit go-ahead).
  - **Builder smoothness (all inside `src/ui/features/combo-builder.ts` + a few CSS lines):**
    - **Amount inputs are now live** — `input` events update that row's kcal chip AND the totals card on every keystroke WITHOUT rebuilding `#combo-builder-items`. Previously every amount change rebuilt the whole list on `change` (= blur), which dropped focus and dismissed the Android keyboard mid-edit — the main "not smooth" culprit. Invalid/empty amounts snap back to 100 only on `change` (commit), never while typing.
    - **Ingredient search is debounced (~160 ms)** with the existing generation counter kept as the race guard; Enter in the search field flushes immediately. One query per pause instead of one per keystroke.
    - **Search-results scroll position survives add/remove re-renders** (`preserveScroll` flag) — tapping a food mid-list no longer teleports the list back to the top when the ✓ In Combo badges re-render.
    - **Haptics:** light tick on ingredient add/remove (also on tap of an already-added row), success notification on save — via `@capacitor/haptics`, which was already a package.json dependency AND a registered native plugin (`capacitor.plugins.json`) but had never been used; zero new deps. Web/desktop silently no-op (try/catch).
    - **New-Combo opens focused:** the name field gets focus ~280 ms after open (after the slide-up settles so keyboard and view animations don't fight); Enter in the name field saves.
    - **Double-save guard:** `btn-save-combo` disables itself and shows "Saving…" for the duration of `saveComboFromBuilder`; restored in a `finally`. A double-tap can no longer create duplicate combos.
    - **CSS polish:** `.combo-build-row` gains a quiet 140 ms entrance animation (transform+opacity only), add-rows get a `:active` scale press feel and a border-color transition.
  - **main.ts decomposition (move-only refactor, user-requested "change the structural risk"):**
    - The 3,203-line god file is now a **391-line composition root**: initApp (context construction + setup call order preserved exactly), setupNavigation (dock/gear/tab controller), setupModals (water quick buttons, manual/scanner modals, score explainer), setupActionHandlers (goal save, manual log, export button, select-history-date listener), dashboard text bar + numpad, bootstrap.
    - New shared layer: `src/ui/context.ts` (**AppContext** singleton — repos/services/dbManager + `currentViewId`/`tabController`/`foodCache` (the old builderFoodCache) + shared `resolveFoodCached`); `src/ui/app-refresh.ts` (refreshStateForDate, dataVersion/bumpDataVersion/invalidateHistoryWindow, history-window cache + ensure/renderHistoryView/shift/setHistoryView + setupHistoryViewHandlers, previously folded into setupNavigation); `src/ui/modal-layers.ts` (openModalLayer/closeModalLayer bridge to the nav stack); `src/ui/dialogs.ts` (requestPassword/requestConfirmation/requestGrams/requestName machinery); `src/ui/haptics.ts`.
    - Feature modules under `src/ui/features/`: `index-screen.ts` (INDEX tab state/sorts/search/food rows/combo list + combo detail modal incl. FoodSortKey/ComboSortKey), `combo-builder.ts` (+ all smoothness above), `logging-actions.ts` (logTextInput/logFoodAtAmount/quickLogFood/logCombo), `scanner.ts` (barcode local→OFF→label fallback chain, label OCR, camera capture, browser live feeds), `journal.ts` (renderJournalIfVisible + day-note/water-edit/bulk-date support handlers + setJournalWaters), `edit-log.ts` (full-screen edit view), `data-tools.ts` (export modal, CSV import, backup/restore, delete-all), `p2p.ts` (+ new `initP2PServices()` so the ICE-server assembly incl. default STUN stays next to the TURN config it reads).
    - Deliberate ESM cycles exist (app-refresh ↔ journal, index-screen ↔ combo-builder) but reference each other ONLY inside function bodies — verified safe by tsc, vite build, and a headless boot smoke test.
    - **Verification:** `npm run build` ✅ (tsc strict), `npm test` ✅ 262/262 (no production code touched outside src/ui), plus headless Chrome against `vite preview`: "Initializing EverydayFuel" → "SQLite database ready" logged, DOM intact, zero console errors/uncaught exceptions.

- **Pass 22 additions — UI feedback round on top of pass 21 (user review after testing pass 21 on device):** installed via adb; build + tests green.
  - **"Can't see all my index in the combo maker" — real bug, fixed:** ADD INGREDIENTS was hard-capped at `getAllFoods(60)` (fuzzy path `40`). Now fetches up to **1000 foods A–Z** (`localeCompare` on canonical_name) when the query is empty and **300 fuzzy hits** while searching — every library entry is reachable by scrolling or search.
  - **Tap-to-toggle ingredients:** tapping a food that is ALREADY in the combo now REMOVES it (`toggleComboBuilderItem`) instead of doing nothing — same result as its × button. Haptic tick fires on both directions.
  - **Builder modernization (CSS):** totals card 16px radius with a gradient wash + border; ingredient/add rows 12px radius (build rows get surface-light fill); amount inputs gain a green focus ring (`:focus` border + soft shadow); ✓ In Combo badge tinted green (bg/border/text) on added rows; remove button scales on press.
  - **Continuous calorie-ring gradient (user spec, replaces the lap-class flat colors):** `ring.ts` rewritten around a PALETTE of anchors walked by the kcal ratio — deep red at 0 → amber at ~50% → light green entering 85% → deep green at target → red overlapping/overtaking the green just past it (1.0–1.15) → violet from ~1.45 → purple intensifying toward 3× over. Colors interpolate via `lerpColor`; a 3-stop SVG gradient (start / mid / leading-edge color) is applied to EVERY render regardless of lap, so over-target days stay gradient-driven. The background track keeps its lap classes (bg-success/bg-over-1/bg-over-2).
  - **Score −1 color:** `--score-neg-1` #fca5a5 (pink-ish red-300) → **#f87171** (light but properly RED, red-400). Pure CSS variable change; tier names/legacy contract untouched.
  - **Import progress bar:** new `#import-progress-modal` (non-dismissable mid-run) with `.progress-track/#import-progress-fill` bar + `n / total rows · k skipped` label; updated per row with an explicit renderer yield every 5 rows so it paints smoothly on the WebView.
  - **Duplicate logged combos:** combo breakdown actions are now Close / **Duplicate** / Edit Combo / Delete All — Duplicate re-logs every ingredient log same day/same amounts through `logRepo.duplicateLog` (new `onDuplicateCombo(cluster)` callback, journal side confirms nothing — instant like single-log duplicate).
  - **Select All in logs:** select-mode header gains an **All** button next to Cancel — force-selects every loaded id in one tap (foods, waters, all combo member logs deduped via `onSelectAll(allLoadedIds)`); bulk move/dup still filters waters out, delete handles them.
  - Files: `src/ui/components/ring.ts`, `src/ui/features/{combo-builder,data-tools,journal}.ts`, `src/ui/views/day-detail.ts`, `src/index.html` (+progress modal), `src/style.css`. No data/domain/test changes.

- **Pass 22 follow-up — user review of pass 22 on device ("gradient fucking sucks / looks like the lgbtq flag", "actually choppier than before"):**
  - **Ring = ONE color at a time (rainbow killed):** the 3-stop arc gradient made the ring show red+amber+green simultaneously. `ring.ts` rewritten again — the stroke is now a SINGLE solid color walked along PALETTE anchors by ratio (`ringColor(ratio)`, set directly via `style.stroke` so the CSS `transition: stroke .3s` smooths shifts between renders): deep red empty → red lifting → amber ~60% → green entering band → deep green through target → red past it → violet ~1.5× → purple intensifying to 3×. Gradient machinery removed entirely; track keeps lap classes.
  - **Builder choppiness root-caused + fixed:** pass 22 raised ADD INGREDIENTS to 1000 rows and rebuilt them all on every tap/keystroke — that DOM churn WAS the jank. Now renders at most **100 rows** (`LIST_RENDER_CAP`) with a "+ N more — type to narrow down" tail; search remains generous behind the cap. Also removed the `.combo-build-row` entrance animation that replayed on every rebuild.
  - **Amount steppers:** − / + buttons flank the grams input (±10 per tap, min 1, haptic tick, updates row kcal + totals live without rebuild/keyboard).
  - **`--score-neg-1` set to the user's exact color `#fc4c4c`.**
  - Gotcha recorded: `npx cap sync android` MUST run from repo ROOT — running it from `android/` silently warns and skips the asset copy (first 22b APK shipped stale assets for one install before being caught and redone).
  - Files: `src/ui/components/ring.ts` (rewritten), `src/ui/features/combo-builder.ts`, `src/style.css`.

- **Pass 22 follow-up 2 — second user review ("duplicate duplicated what's INSIDE, not the combo log"; builder still laggy + disliked UI; purple timing):**
  - **Combo Duplicate bug root-caused:** `logRepo.duplicateLog` copies `observation_id`, so duplicating combo members re-clustered into the ORIGINAL group (one "6 items" row) instead of a second standalone combo row — exactly what the user saw. `onDuplicateCombo` now inserts a FRESH combo observation marker (`source_type:'combo'` + interpretation JSON) and `insertFoodLog`s each member against it → the duplicate appears as its own "🍱 name" log entry.
  - **Builder lag fix #2:** the A–Z library was still re-fetched (1000 rows over the SQLite bridge) + re-sorted on every add/remove tap. Now cached once per builder session (`builderLibrary`, invalidated on open/close); taps render purely from cache.
  - **Builder redesign:** layout reordered — the combo being built (name → live totals → INGREDIENTS list → Save) is now ONE card up top; ADD INGREDIENTS search moved to its own card BELOW. Name field restyled as a large borderless underline input (accent on focus). New classes `.cb-main-card/.cb-name-input/.cb-items/.cb-save-btn/.cb-add-card/.cb-results`; orphaned old markup with duplicate IDs removed.
  - **Ring purple timing:** palette holds pure red 1.2→1.75, blends to violet ~1.75–2.05 (arrives around a FULL extra circle), then intensifies to deep purple by 3×.
  - Files: `src/ui/features/{journal,combo-builder}.ts`, `src/index.html`, `src/style.css`, `src/ui/components/ring.ts`.

- **Pass 22 follow-up 3 — third user review (dead sort toggle, builder scroll flow, "Close" UX, combo macros, import accuracy, app-wide tap lag):**
  - **Newest/Oldest toggle was dead** — the §5d window is a single selected day, so sorting DAY groups had nothing to reorder. It now orders the ENTRIES within the day (chronological ⇄ newest-first).
  - **Builder flow:** ADD INGREDIENTS moved back up directly under the totals card; INGREDIENTS + Save live in their own card below with a **sticky Save button** (`position: sticky` to the bottom of the scroller) — adding foods never means scrolling down again.
  - **"Close" removed from logged combos** — tapping the row collapses it like every other log item.
  - **Combo macros on the log row:** total P/C/F now render on the collapsed combo row, same `.log-macros` profile as food logs.
  - **Legacy CSV split: equal → amount-weighted.** The user's example ("Shark meat (120g)" showing 8P/13C/8F instead of ~25P/0C/5F) was the equal-split design: on a 3-food day every item got ⅓ of the day totals. `parseCSV` now parses trailing `(NNNg)`/`(ml)` amounts from each Logs segment and splits day totals **proportionally to grams** (equal shares only when no amounts exist); `amountG` per row uses the parsed amount. Single-food days were already exact. **Honest limit: per-item macros do not exist in the legacy export — multi-food days remain estimates (`estimatedSplit`); nothing can make them exact without external data.** Sums stay byte-equal to day totals; all csv-import tests pass unchanged (fixtures carry no "(g)" suffixes).
  - **Tap-lag optimization pass (user: "laggy when i click on something"):**
    - `refreshStateForDate`: five independent reads (goal/totals/water-sources/logs/waters) now run via one `Promise.all` instead of six sequential bridge round-trips — this runs after EVERY action so serial latency here was global tap lag.
    - The §5a recents debug query no longer runs per refresh (gated behind `window.__EF_DEBUG__ === true`).
    - INDEX screen fetches are cached per query (`indexFetchCache` + `combosCache`) and cleared by a store subscription after any data change → expand/collapse/sort/tab taps are pure memory renders.
    - Store subscribers only re-render the ACTIVE view (hidden dashboard/goals renders skipped); each view renders itself on tab switch instead.
  - Files: `src/services/import/csv-import.ts`, `src/ui/views/day-detail.ts`, `src/ui/features/{index-screen,combo-builder}.ts`, `src/ui/app-refresh.ts`, `src/main.ts`, `src/index.html`, `src/style.css`.

- **Pass 22 follow-up 4 — fourth user review + "saving doesn't work" ROOT-CAUSED via live CDP debugging on device:**
  - **THE BUG:** combos WERE created/deleted in SQLite all along, but the pass-22c index caches kept serving pre-mutation lists — saved combos never appeared (and deleted ones never left). **Fix:** exported `invalidateIndexCaches()` from index-screen; called after combo save (builder), combo delete (detail modal), CSV import, backup restore, delete-all, and P2P receive. **Verified on-device over CDP**: save → new combo row appears instantly; delete → row vanishes instantly.
  - **Dev-tooling finding (not a user bug):** `location.reload()` over CDP left the WebView's named SQLite connection returning empty sets (UI showed 0 foods/combos while a raw plugin query on the SAME page saw 573 foods) until a real `am force-stop` cold start. Normal icon launches are unaffected; logged as investigation candidate (#1 in the open list).
  - Auto-focus on the combo name field REMOVED (user request — keyboard yanked them out of the ingredient flow).
  - Search inputs restyled as pills (`.search-pill`, green focus ring) on INDEX + builder.
  - INDEX render capped at 120 rows (+ "N more — search to narrow down" tail): full-list rebuilds were the remaining tap jank.
  - Swipe-driven tab changes fire a light haptic tick (`nav.ts` touch handler).
  - Imported-split confidence chips read **"estimated"** instead of the fake-precise "50% sure".
  - Files: `src/ui/features/{index-screen,combo-builder,data-tools,p2p}.ts`, `src/ui/nav.ts`, `src/index.html`, `src/style.css`.

- **Pass 23 — user request "remove encrypted p2p, keep backup to device; save next to close; editing food; nicer filter" + high/medium/low sweep:**
  - **P2P + TURN removed:** deleted `src/ui/features/p2p.ts` + `src/services/transfer/*` (protocol/qr-code/transfer/transport/webrtc) + `qrcode` deps, removed P2P modals (`#p2p-modal`, `#qr-scan-modal`) and Peer-transfer buttons/TURN config from `src/index.html`, removed `initP2PServices`/`setupP2PHandlers` and `p2pService`/`p2pTransport` from `src/main.ts`/`src/ui/context.ts`. Backup-to-device (Encrypted Backup Archive / Restore) remains — file-based only.
  - **Combo Save moved top-right:** Save button now lives in the builder header (`cb-header` flex, `cb-save-btn-top` pill, `Save` label) next to ‹ Back — no scrolling to save. Removed bottom sticky `.cb-save-btn`.
  - **"Editing a food doesnt update it" — FIXED:** added Food Edit modal (`#food-edit-modal` in `src/index.html`) + `openFoodEdit`/`saveFoodEdit` in `src/ui/features/index-screen.ts` (updates `FoodRepository.update`, normalizes name via `normalizeFoodName`, handles UNIQUE, busts `ctx.foodCache` + `invalidateIndexCaches` + `saveWebStore`, re-renders index). Expanded food rows now show **Edit** alongside Log 100 g / Other amount…. Verified on-device: edit "Farmfrite…" 147→150 kcal updates instantly and persists after reload; revert works.
  - **Index filter nicer:** wrapped search + toolbar in `.index-controls` card (rounded, bordered, padded), toolbar now `flex` with tabs `flex:1` + sort as pill (`border-radius:999px`, `min-width:130px`). No longer a full-page stretched bar.
  - **High:** warm-reload DB race — added `initPromise` mutex + `checkConnectionsConsistency`/`retrieveConnection` reuse + close-before-create guard in `src/data/database.ts`; `getConnection` now awaits `initPromise`. Prevents the CDP `location.reload()` empty-DB race.
  - **Medium:** ring 100%/200% empty bug — fixed `renderCalorieRing` (`src/ui/components/ring.ts`) to treat `ratio %1 ===0` as full lap (`strokeDashoffset 0` not 314); water source strings `Explicit`→`Water`, `Drink`→`Drinks` (kept `Food`); scanner label-OCR fallback collapsed into `<details>`; backup restore now calls `saveWebStore` + clears `foodCache`, CSV import also calls `saveWebStore`, backup-restore non-atomic web warning remains but is now flushed to IndexedDB; index search debounced (180 ms) to reduce re-renders.
  - **Low #9:** PWA installability — added `src/public/manifest.json` + `src/public/sw.js` (skipWaiting + claim), `<link rel="manifest">` + `theme-color` in `src/index.html`, SW registration in `src/main.ts` (`!Capacitor.isNativePlatform()` guard). Offline core already worked; now installable.
  - Verified on-device (OPPO CPH2363, CDP): P2P gone, Save in header, index 120 cap, food edit round-trip (edit→150→revert), water labels "Water/Drinks/Food", scanner has `<details>`, combo top-save round-trip (build→1 ingredient→Save header→1 combo→delete→0), manifest link present, ring 100%/200% =0.
  - Files: `src/{index.html,style.css,main.ts}`, `src/data/database.ts`, `src/ui/{context.ts,components/ring.ts,nav.ts,views/day-detail.ts,features/{index-screen,combo-builder,data-tools,scanner}.ts}`, `src/public/{manifest.json,sw.js}`, `package.json`, `src/services/transfer/*` deleted.

## 3. Verification status

| Check | Result |
|---|---|
| `npm run build` (tsc strict + Vite) | ✅ passes (197 kB index-KWeHL…→Dh1u…, 27.8 kB CSS, 0.9 kB sw) |
| `npm test` (vitest 4.1.11, **238 tests**, 16 files — P2P/QR tests removed: 262→238) | ✅ passes (run 2× to confirm no flakes) |
| CSV import of the old app's export (csv-import.test.ts, 12 tests — legacy daily-aggregate shape: actuals-not-targets, Pure Water → waterMl, Logs → food name; plus the real `exportexample.csv` file: all 96 days parse, spot-checked totals 1282/65/181/30 and 1855/72/248/66) | ✅ passes — the "imported wrong" report is reproduced by test (pre-fix it read Target columns) and fixed; import-click-through in the UI still needs a browser (folded into item 2's device pass) |
| Export service (export-service.test.ts, 12 tests on real sql.js SQLite: date lists, all-time bounds, per-phase goal resolution, hydration gating, legacy score contract, low-accuracy/note, empty-day skipping, 21-column CSV round-trip) | ✅ passes — the export modal's logic is fully covered; the modal DOM itself ❌ NOT clicked through (no browser) |
| Fallback shim evaluator (database-shim.test.ts, 12 tests: positional INSERTs, UPDATE, ORDER BY/LIMIT, GROUP BY, COUNT, ON CONFLICT + excluded.*, JOIN enrichment, DELETE) | ✅ passes — the evaluator is now an exported factory tested directly, closing the fallback-mode gaps that caused "edit doesn't persist" / "goal config not saving" in fallback mode |
| History window derivation (history-window.test.ts, 12 tests: range/month/year dates incl. leap years, hasData gating, per-day score/totals/hydration, hydration gating) | ✅ passes |
| Scoring regression vs legacy export (scoring-regression.test.ts, spec §29) | ✅ passes — 95/96 rows of `exportexample.csv` reproduce `score`/`scoreCode`/`scoreTier`/`result`/`reason` byte-for-byte; the 1 non-replayable row (2026-05-03) is asserted + documented |
| Real-SQLite migration + repository + backup/restore round-trips (sql.js in-memory engine, migration.test.ts + sqlite-real.test.ts) | ✅ passes — all repository SQL (JOINs, GROUP BY, LIKE, SUM, bound LIMIT, ON CONFLICT) proven against a real SQL engine, including FK-safe restore ordering with `PRAGMA foreign_keys = ON` |
| Encrypted backup (encryption.test.ts, 17 tests: PBKDF2→AES-GCM round-trip, tamper detection, wrong-password, envelope structure) | ✅ passes |
| FoodService pipeline (service test w/ in-memory connection) | ✅ passes — text + label-OCR paths (food/observation/log/water creation, library reuse, validation) |
| `collectAllTables` on a real SQLite database (sqlite-real.test.ts) | ✅ passes |
| Backup/restore round trip + restore atomicity/rollback (backup.test.ts + sqlite-real.test.ts) | ✅ passes |
| Gemma fallback text + label-OCR parsers (15 tests) | ✅ passes |
| Laptop-view CSS (Phase 10) — build output verified to contain the new `#history.active-view` / `#view-goals.active-view` / `#day-view-container` rules; mobile (<768px) rules untouched | ✅ passes (static); visual click-through ❌ NOT run (no browser) |
| Offline audit (Phase 11 static) — `src/` contains zero network calls; only touchpoint is the Google Fonts `<link>` (graceful fallback) | ✅ passes (static); network-off click-through on a device ❌ NOT run |
| Vision client (vision-client.test.ts, 8 tests: plugin detection, data-URL stripping, format list, Node-side unsupported detection + graceful rejection) | ✅ passes |
| **Native Android build** (`gradlew assembleDebug`, JDK 21, Kotlin 2.2.20) — `app-debug.apk` (89 MB) produced; `:app:compileDebugKotlin` compiles GemmaPlugin.kt + VisionPlugin.kt; the camera plugin's `jvmToolchain(21)` requirement satisfied | ✅ passes (compile verified; on-device camera click-through ❌ NOT run — no device) |
| **On-device verification, pass 14 (OPPO CPH2363, USB + WebView CDP)** — app launch + dashboard on native SQLite; text log; label OCR end-to-end (ML Kit → parse → log, library reuse); barcode camera/gallery paths (Prompt chooser); History 7D/1M/1Y + paging + stats; airplane-mode water/text/known-food flows; online barcode fallback (real OFF fetch → log + save; airplane-mode rescan → instant local hit; unknown+offline → label-scan fallback with camera chooser) | ✅ all pass (see pass-14 log for per-check details); logcat clean |
| **On-device verification, pass 15 (OPPO CPH2363, USB + WebView CDP)** — §5a items 1–4, 7, 8: recents gone from UI (textbar card present); text bar logs + clears; Journal/Manual top-left + Settings top-right (no status-bar overlap, ring card 52px, dashboard fits one 800dp screen); label name modal (placeholder → user-named "Test Granola" logged); barcode grams modal (Nutella 50 g → 269.5 kcal, `amount_g: 50` in SQLite); grams Cancel aborts cleanly; `console.debug` recents hook fires on every refresh | ✅ all pass (see pass-15 log for per-check details) |
| **On-device verification, pass 16 (OPPO CPH2363, USB + WebView CDP)** — viewport-fit/layout: `main paddingTop 100px` / `sysTop 64px` (env 36) no overlap, dock `730-800` scan `733.5-797.5` inside bar; History perf: month 21 ms (31 `cal-block`s) / year 25 ms (12 `month-block`s, 365 `day-dot`s) CDP stays responsive; batch parity verified on real SQLite | ✅ all pass (see pass-16 log) |
| **Pass 18 verification (OPPO CPH2363 + PC Chrome via CDP)** — pass-17 layout metrics reproduced + screenshot taken; export modal all four scopes with content checks (post-fix file in `/sdcard/Download`, pulled + inspected); inline expand/edit-screen/back; water add+delete (incl. offline); day notes set/clear; multi-select dup+delete round trip; modal-BACK; swipe; combos create/log/cleanup; **P2P phone↔Chrome transfer + restore end-to-end**; airplane-mode offline pass; laptop-layout screenshots (dash/logs/goals ≥768px); DST + trailing-day regression tests | ✅ all pass (see pass-18 log) |
| **Pass 19 verification (OPPO CPH2363 via adb + WebView CDP, real gestures/keys)** — hardware `KEYCODE_BACK` closes edit screen in-app (dock returns); REAL swipe-back gesture navigates LOGS→DASH without exiting (previously fatal — root-caused: Capacitor 8 core has zero back handling, @capacitor/app's OnBackPressedCallback now intercepts both back paths); tab-swipe DASH↔LOGS with direction animation class observed then auto-cleaned; edit view full-screen (`body.edit-open` hides dock+gear; ‹ Back top=112px, no status-bar collision; prefilled); journal: 13 day-groups, weekday/date/note sublines, running kcal totals, HH:MM time chips, P/C/F rows, selected-day highlight, Load Older button; combo builder create→save→Log All(+365 kcal)→journal cluster→breakdown→Delete All cleanup; library breakdown Close/Log All/Edit/Delete on a pre-existing combo; low-accuracy chip toggle round-trip (DB 0→1→0); day-note set/render/clear ("Bom" preserved); goal save updates same row (2500→2550→2500, count stays 1); Selected-Day export pulled from Downloads = 23 columns with Avg 0.84/Min 0.7; Outfit loads from local bundle (`document.fonts.check` true); dashboard header "Today / Score: N" above ring with 30px pull-up gap (screenshots taken) | ✅ all pass (see pass-19 log) |
| Manual click-through on device/browser | **Remaining:** hardware BACK/swipe feel (human pass), bulk Change Date actual move, QR camera pairing cross-device (paste path verified), real-barcode Grams re-check after UI changes (unchanged code paths) |

## 4. Known limitations (updated pass 19)

- **P2P/TURN removed (pass 23):** the encrypted P2P transfer (WebRTC + QR + TURN) was removed per user request — only **Backup-to-device** (Encrypted Backup Archive file) remains. Previous P2P verification (pass 18) is now obsolete.
- **jeep-web transactions:** the WASM connection doesn't hold BEGIN across run() calls; backup restore on WEB commits non-atomically (warned in console) after a mid-write failure would leave partial data — pass 23 now flushes via `saveToStore` + `saveWebStore` after restore/import, but atomicity remains best-effort on WEB. Native Android restores are properly transactional.
- Fallback connection is a degraded shim — pass 10 closed the reported gaps (covered by `database-shim.test.ts`); unknown statement shapes still degrade to no-ops. Restore in fallback mode replaces the store wholesale (correct). The real-SQLite suite proves all repository SQL works on the real engine; confidence columns export empty there by design.
- Backup archives are **encrypted by default** (PBKDF2 + AES-256-GCM, pass 5); password/confirm use **in-app modals** which close correctly via Android BACK (layer stack + @capacitor/app since pass 19).
- Native ML Kit label OCR + camera barcode scanning implemented + on-device verified (pass 13/14). Unknown-barcode fallback chain: local library → Open Food Facts → label-scan fallback. Food-image analysis (spec §7.2) NOT implemented — §5c item 1, deferred to the next pass.
- On-device Gemma inference still needs the model file pushed (user is evaluating options — §5b item 9); deterministic fallback parsers active meanwhile.
- Label-OCR kJ-first quirk FIXED in pass 19 (+ regression tests updated).
- Scoring regression blind spot for `2026-05-03` unchanged (documented, locked by test).
- Zero-target semantics preserved as-is (pre-existing).
- Offline: **zero network touchpoints remain** — Google Fonts replaced by bundled Outfit (pass 19); PWA manifest + minimal SW added in pass 23 for installability (no network fetch, offline core unchanged).
- Export skips empty days (legacy behavior); confidence columns added in pass 19.
- CSV legacy import now splits each day per food **weighted by parsed `(120g)` amounts** (fallback to equal shares when no amounts; pass 22 follow-up 3); single-food days exact, multi-food days remain estimates (`estimatedSplit`); adds without dedupe (unchanged).
- Pass-19 UI notes: journal "Load Older Days" paging is one-directional (no collapse back); combo ingredient amounts are fixed at 100 g/ml in templates (breakdown/edit reflect that); pre-existing duplicate combo templates in the user DB were left untouched (user data sacred).
- Pass-21 notes: builder smoothness changes are verified by build + tests + headless boot smoke test only — **on-device feel check (keyboard persistence while typing amounts, haptics, focus/Enter-save) still outstanding**; the decomposition moved code but kept every setup call and listener registration order equivalent.
- Pass-22 notes: ring gradient is a linear-gradient projection over the SVG bounding box — the color ORDER along the arc is faithful (red→amber→green→red→purple) but exact angular stop positions are approximate (inherent to linearGradient on a circle). Import progress modal is intentionally not BACK-closable mid-run (a layer was deliberately NOT pushed); killing the app mid-import leaves partial rows — same as pre-pass-22 behavior, now at least visible.
- Pass-23 notes: ring at exactly 100%/200% now shows full (not empty) via lapOffset fix; water badges now "Water/Drinks/Food"; scanner label-OCR paste fallback is collapsed (`<details>`); index search debounced 180 ms + render cap 120 + controls card; warm-reload DB race closed via `initPromise` + `checkConnectionsConsistency`; food edit modal verified on-device; combo Save top-right verified; PWA manifest/SW added.

## 5. What's next (from PLAN.md §9 handoff)

1. ~~Browser/device verification of the P2P flow~~ — **REMOVED in pass 23 per user request** (was DONE in pass 18 over LAN). Only Backup-to-device remains.
2. ~~Human UX pass on the new interaction model~~ — **machine-verified in pass 19** (real `KEYCODE_BACK` + real swipe gestures over adb navigate in-app via @capacitor/app); a final human feel pass on animation timing is still welcome.
3. ~~Full native ML Kit integration~~ — done (pass 13, device pass 14). Food-image analysis (spec §7.2) remains a separate later phase.
4. Phase 11 offline validation — airplane-mode spot checks done (pass 14 + pass 18: water/text/history offline). Optional deeper offline pass: goals editing, export-to-Downloads, backup/restore while offline.
5. ~~§5b items 9–11~~ — **items 10–11 (ring-card pull-up + date/score header) done in pass 19**; item 9 (AI model file decision) remains user-driven.
5. ~~Laptop/desktop view~~ — done in pass 9 (Phase 10, spec §25): History + Goals views now present properly on ≥768px screens (pure CSS; mobile untouched). Visual click-through still outstanding on a real browser (listed under item 2's device pass).
6. ~~More regression tests~~ — closed (see §5 item 5 in the section above).
7. ~~Restore-from-backup UI~~ — done in pass 3 (Settings → Restore from Backup Archive)
8. ~~Encrypted backup format~~ — done in pass 5 (spec §23; see §2/§4). ~~Replace `window.prompt` password entry with a proper in-app modal~~ — done in pass 7 (password + confirm modals wired).
9. ~~P2P transfer~~ — JS/service/UI done in pass 8 (spec §23; see §2/§4) — **manual device verification outstanding (see item 1)**.
10. ~~Date-range / all-time / goal-phase CSV export (spec §21, QoL #9/#10)~~ — done in pass 11 (see §2); visual click-through of the modal still outstanding (folded into item 2's device pass).

## 5a. User requirements backlog — pass-14 user review

> Items 1–4, 7, 8 were **implemented and on-device verified in pass 15** (see §2). Items 5–6 remain "later stage, once everything is confirmed" — **noted only, NOT implemented**. User wording preserved where useful.

**Dashboard layout** — DONE (pass 15)
1. ~~Remove 1-Tap Recents from the UI~~ — done; the query stays as a `console.debug('[debug] recents (UI hidden):', …)` hook (agent-visible via WebView CDP, never in the UI).
2. ~~Move the text input into an actual text bar~~ where 1-Tap Recents currently sits — done: always-visible `.textbar-card` (`#dash-text-input` + LOG) on the dashboard.
3. ~~Move Journal and Manual to the top-left~~, opposite the Settings button — done in pass 15; **superseded in pass 17**: the floating pills are gone, Journal/Manual now live as an integrated `.textbar-utils` row inside the dashboard textbar card (user request: "integrate them into the app… like a dashboard util").
4. ~~Reduce the overall UI length~~ — done: compressed paddings/gaps/ring; buttons below the status bar (top 20px on device), no overlap, dashboard fits one screen.

**Scanning UX (later stage, once everything is confirmed)** — NOT implemented
5. **Keep the "From Photos / Take Photo" chooser for now as a debug aid.** Later, make **barcode scanning automatic like the old app**: the moment the camera sees a barcode it finishes by itself (no pressing "take a picture" every time). **Nutrition label stays manual**: the button press to take the picture is kept.
6. **Never ship** the OCR-fallback paste-label-text path (`label-ocr-text` textarea) and the manual barcode-number lookup (`barcode-input`) — the user doesn't want them at all; keep them only for the dev cycle.

**Product naming** — DONE (pass 15)
7. ~~"Scanned Label Product" placeholder must go~~ — done: label scans use the real product name (OCR heading/keyword extraction in the fallback parser); unknown name → ask the user (in-app name modal) for a **nutrition label**; **barcode** name is automatic (online lookup) — no asking.

**Grams panel** — DONE (pass 15)
8. ~~Add a grams-amount panel for BOTH label and barcode scans~~ — done (old_app parity): per-100g nutrition from the barcode/label record; **the grams the user enters are what THEY ate** (grams modal with default 100, Cancel aborts cleanly; label path asks name first if unknown, then grams).

**Not-yet-developed notes (recorded for context)**
- Gemma on-device inference still needs the model file pushed (deterministic fallback parsers are used until then).
- Food-image analysis (spec §7.2 geometry/grams) is a separate later phase.

## 5b. User requirements backlog — post-pass-17 review (2026-08-21)

> **Items 1–8 were implemented in pass 18. Items 10–11 (ring-card pull-up, date+score header) were implemented in pass 19. Item 9 remains user-driven.**

**Log interaction & navigation rework**
1. Tapping a log should **expand it inline** with the actions shown right there — replace the action-hub modal (`#action-hub-modal`) entirely.
2. **Edit opens a dedicated screen/view** (full view, not the current `#edit-modal` popup).
3. **Android back button / swipe-back must navigate back inside the app** (to the previous view/state), never exit the app — implies Capacitor `backButton` handling + an in-app view stack.
4. **Re-add swipe gestures between tabs** (DASH / LOGS / GOALS) — existed in spirit in the old app; `main` still carries a leftover `transition: transform` from it.
5. **Day notes UI is missing** (QoL #5): daily_records note exists in data + export, but nothing in the UI writes/edits/shows it.
6. **Multi-select for logs** — bulk actions limited to: **change date, delete, duplicate** (explicitly NO bulk edit).
7. **Combos UI missing** (QoL #13): domain/repo support exists since pass 2 (`expandCombo`, combo repo), but there is no UI to create or apply combos.
8. **Log/food notes don't render on log items** in the day lists — the note is editable in the edit modal but never displayed.

**AI model (user-driven, in progress)**
9. User is evaluating options for the Gemma model file — storage size is the concern ("a headache"). Deterministic fallback parsers remain active meanwhile; no agent action until the user lands on an approach.

**Dashboard layout (user-approved proposal)**
10. Pull the **calorie-ring card up** (ONLY that card + its box), opening visible space between it and the PRO/CARB/FAT bars card.
11. If space allows after that: **date header + day score above the ring card** (agent-proposed, user approved the concept).

## 5c. User requirements backlog — pass-18 review (2026-08-22, post-commit `8b69c6d`)

> **IMPLEMENTED in pass 19 — everything below except item 1 (food-image pipeline, deferred to next pass) and item 2 (Gemma model, user-driven).** Item 5 (QR cross-device) stays parked; items 8/9/14 need no action. Numbering follows the agent's 16-point "what's missing" scan; the user answered each point, then added new UI feedback (A–F).

**Decisions on the gap scan:**
1. **Food-image pipeline (spec §7.2)** — GO ("Yeah").
2. **Gemma model file** — user still working on it (storage tradeoff); agent hands off.
3. **Low-accuracy flag UI** — GO (set/show `daily_records.low_accuracy`).
4. **Confidence metrics export column** — ADD the column(s). **PLUS an import fix (user-reported):** importing the legacy `exportexample.csv` collapses each day into ONE food log holding the day's total macros (documented pass-12 behavior) — user wants this fixed so imports don't fabricate a single mega-item per day.
5. QR cross-device camera scan — "ok" (parked).
6. ~~Bulk Change Date~~ — works, user-confirmed ✓.
7. Hardware BACK/swipe — functionally works but UX is "meh" → improve with animation + better transition UX.
8. Cross-network TURN pairing — DON'T (unneeded complexity); keep LAN-only focus.
9. Offline deep pass — user handles.
10. Service worker — consult spec first (§24 requires offline core workflows, which already work; sw.js was old-app PWA nicety). Low priority.
11. **Bundle the Google Font locally** — GO (removes the only network touchpoint).
12. **`btn-save-goals` new-goal-per-save** — fix if it's bad UX (it is): make save update the active goal / show goal history properly.
13. **kJ-first label OCR quirk** — FIX it now; update the regression test alongside.
14. jeep-web non-atomic restore — acknowledged, no action needed.
15. **§5b items 10–11 (ring-card pull-up + date/score header above ring)** — GO.
16. Dead `state.selectedLogForAction` field — clean up freely.

**New UI feedback (screenshots reviewed):**
A. **Edit screen layout broken:** content starts too high (‹ Back collides with the status-bar clock — missing safe-area padding), and it's not genuinely a dedicated screen — the dock (DASH/LOGS/scan) and settings gear are still visible while editing. Fix: proper full-screen edit view (hide dock + gear, correct top padding like other views), plus general visual polish.
B. **Swipe-back exits the app** from any screen instead of navigating in-app — the History-API approach does NOT intercept the Android predictive-back gesture on this device. Needs real backButton handling (e.g. Capacitor App plugin or disabling predictive-back opt-in) — revisit the earlier decision to avoid @capacitor/app.
C. **Combos rework:** user CANNOT create combos in practice (only delete works) — creation flow is broken/unusable; fix it. Combos should be saved AND viewed like screenshot #2: tapping a logged combo expands into a detail card ("NOURISHMENT BREAKDOWN") listing each ingredient with amount/macros/kcal and Close/Edit/Delete actions. Journal presentation of combos needs a redesign (current looks bad, doesn't work well).
D. **Logs list redesign** (screenshot #3 = old-app journal style): chronological ordering with the **time shown next to each logged item**, day-group headers (weekday + date — running day total kcal), per-item kcal + P/C/F profile as in that mock.
E. **Dashboard §5b items 10–11 (GO, see #15):** pull the calorie-ring card (only that card + its box) up; add date + day score in the space above it.
F. **Day-note row UX:** the whole bar must be clickable to edit — not just the small ✎ button at the far right.

## 6. Before you start

- **Pass 21 + 22 + 23 are NOT committed yet** — awaiting user go-ahead per governance §15 (one combined commit for passes 21-23). Pass-21 files: rewritten `src/main.ts` (3,203 → 391 lines); new `src/ui/context.ts`, `src/ui/app-refresh.ts`, `src/ui/modal-layers.ts`, `src/ui/dialogs.ts`, `src/ui/haptics.ts`, `src/ui/features/{index-screen,combo-builder,logging-actions,scanner,journal,edit-log,data-tools,p2p}.ts`; `src/style.css`. Pass-22 files on top: `src/ui/components/ring.ts`, `src/ui/features/{combo-builder,data-tools,journal}.ts`, `src/ui/views/day-detail.ts`, `src/index.html` (+`#import-progress-modal`), `src/style.css`. Pass-23 on top: removed `p2p` + `transfer` + `qrcode`, added `src/public/manifest.json`+`sw.js`, moved combo Save top-right, food edit modal, index filter card, ring/water/scanner/DB/PWA fixes; tests 262→238, build 197 kB. Earlier commits: pass 20 + follow-ups = `53f9925`/`a11ede8`/`fc61b82`, pass 19 = `11df140`, pass 18 = `8b69c6d`.
- **Module map (post-pass-21):** `main.ts` = composition root only (initApp + navigation + dashboard-level listeners). Shared state goes through the `ctx` singleton in `src/ui/context.ts`; per-view state is module-private in its feature file. When adding a feature: create `src/ui/features/<name>.ts`, export a `setupX()`, call it from initApp — do NOT grow main.ts again.
- **Committing cadence (user-confirmed, pass 13):** finish work → build/test/diff → **report and wait for explicit go-ahead** → commit (`feat(scope): ...`) + push; record hash in the pass log + here.
- `vitest`: **238 tests / 16 files** (P2P/QR tests removed in pass 23). sql.js tests need no config; do not delete `src/types/sql-js.d.ts`. Tests never import from `src/main.ts` or `src/ui/features/*` (DOM-heavy) — they cover domain/data/services.
- AI logs live in `Ai Guidelines/ai logs/logs/` (git-ignored). Pass-19 log: `[ox-alpha][pass 19][2026-08-22].md`.
- **Toolchain:** JDK 21 at `C:\Android\jdk-21\home` required for gradle (`$env:JAVA_HOME = "C:\Android\jdk-21\home"` first); Android SDK at `C:\Android`. `npx cap sync android` from repo ROOT. On-device flow: OPPO CPH2363 (adb `6c447b5a`), `adb install -r`, WebView CDP via `adb forward tcp:9222 localabstract:webview_devtools_remote_<pid>` — helpers recreated this pass in `%LOCALAPPDATA%\Temp\opencode\`: `cdp_eval2.ps1` (-Expr/-File/-Raw/-Port) and `cdp_shot.ps1` (-Port -Out; screenshot works for phone AND desktop Chrome). Gotchas: the CDP socket changes per app start (re-forward after every relaunch); gradle output redirect `*> file.txt` still prints a wrapper kill message on long runs but the daemon completes — check the txt tail for BUILD SUCCESSFUL; hardware back/gestures are drivable from adb (`input keyevent KEYCODE_BACK`, `input swipe x1 y1 x2 y2 ms`) so back-button behavior is machine-verifiable; the phone's TZ is Africa/Cairo (midnight DST transitions — relevant to any local-date arithmetic).
- Read `Ai Guidelines/NutritionOS — Agent Governance & Development Rules.md` before editing; PLAN.md §7 lists what must NOT change (scoring, hydration gating, goal resolution, v001 schema, domain types, CSV export format structure — pass 19 extended the export with 2 appended confidence columns without touching the legacy 21; all else untouched).
- Commands: `npm run dev` (web), `npm run build`, `npm test`, `npm run cap:sync` / `cap:run` (Android).

(End of file)
